;; =============================================================================
;; -- proxy --
;; =============================================================================

;;(setq url-proxy-services
;;      '("http"  . "localhost:8888")
;;      '("https" . "localhost:8888")
;;      )
